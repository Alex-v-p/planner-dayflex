[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "planner-dayflex-worker-service"
version = "0.1.0"
description = "Non-critical background worker for planner-dayflex"
readme = "README.md"
requires-python = ">=3.13,<3.14"
dependencies = [
    "httpx>=0.27,<1",
    "pydantic-settings>=2.6,<3",
    "redis>=5.2,<6",
    "rq>=2.0,<3",
]

[dependency-groups]
dev = [
    "fakeredis>=2.26,<3",
    "pytest>=8.3,<9",
    "ruff>=0.8,<1",
]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 88
target-version = "py313"
src = ["src"]

[tool.setuptools]
package-dir = { "" = "src" }

[tool.setuptools.packages.find]
where = ["src"]
