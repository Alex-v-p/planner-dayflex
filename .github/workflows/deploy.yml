name: Deploy

on:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  deploy:
    name: Deploy production (placeholder)
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Deployment not configured
        run: |
          echo "No deployment target is configured yet."
          echo "Replace this step after selecting the hosting platform."
